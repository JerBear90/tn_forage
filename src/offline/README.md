# Offline Module

Expected responsibilities:
- IndexedDB wrapper
- Sync queue
- Map tile cache helpers
- Offline image storage
- Local profile cache
- Background sync

Offline core must never depend on live network.
