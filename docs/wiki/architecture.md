# Architecture

ForageFlow uses an offline-first PWA architecture.

Core layers:
- UI shell
- Offline data layer
- Auth layer
- Sync queue
- Map layer
- Safety layer
- Membership layer
- Community layer

The app must remain useful without internet.
